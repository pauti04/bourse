[toolchain]
# Pinned for reproducibility. Bump intentionally, with CI green on the new
# version, in a dedicated commit.
channel    = "1.95.0"
components = ["rustfmt", "clippy"]
profile    = "minimal"
