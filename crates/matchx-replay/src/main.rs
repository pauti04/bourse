//! matchx replay tool.
//!
//! Reads `(latest snapshot, WAL since)`, reconstructs the order book, and
//! exits with a state hash. Bootstrap stub — implementation lands in the
//! replay slice (after WAL).

fn main() {
    // Intentionally empty in the bootstrap slice.
}
